---
description: FloatAnchor 开发守则 — 默认启用第一性原理与多 agent workflow
alwaysApply: true
---

# FloatAnchor 开发守则

## 0. 默认工作方式

- 本规则仅服务 `float-anchor` 仓库。
- 在本仓库内进行功能开发、UI 调整、交互改动、体验优化、中大型重构时，默认启用 `.codex/skills/float-anchor-agentic-workflow/SKILL.md`。
- 用户是“总经理”；agent 必须按 `产品经理 -> UI 设计师 -> 研发 -> 测试` 的顺序推进，并允许 `测试 -> 产品/研发 -> 测试` 的回环，直到方案和质量都通过。
- **绝不能把团队内部第一版（`Internal v0`）直接交给用户当“初版”。** 用户看到的“初版”必须是经过产品经理 agent 在交付前严格审核（Pre-Delivery Review）、并明确签收为 `GM v1` 之后的版本。如果产品经理认为内部版本不行，必须打回给对接方（UI / 研发 / 测试），来回打磨直到达到 `GM v1` 标准，再呈交给用户。这条规则同样适用于 lite mode：lite 任务也要做一次 PM 自检 Pre-Delivery Review，只是检查项可以更短。
- 极小任务（如错字修正、纯类型修复、无交互影响的内部重构）可以走轻量流程，但仍不得跳过第一性原理检查、功能验证和 QA 自检。
- 如果当前会话没有自动加载该 skill，需要主动读取并按其执行，不能直接跳进编码。
- 仅对**真实功能需求任务**写本地多 agent 工作流日志：即涉及产品功能、UI、交互、用户可感知行为、功能验证、或实际产品代码改动/验证的任务。日志路径放在 `log/` 目录下，建议格式为 `log/YYYYMMDD-HHmmss-<task-slug>.md`；同一需求跨多轮对话时，持续追加到同一文件。
- **内部 AI 开发基础建设任务**（如修改 workflow 规则、SKILLs、日志制度、agent 提示、但不涉及产品代码与产品行为改动）**不写 log**。这条规则也必须写进项目 workflow skill 中。
- 日志至少必须记录：`用户原始问题`、`本次采用 full/lite mode`、`是否使用真实子 agent 还是主 agent 模拟角色`、`PM 实际使用的 skills`、`UI 实际使用的 skills`、`PM/UI/研发/QA 各阶段产出与交接`、`所有打回与回环记录`、`Pre-Delivery Review 每一轮的判定/打回原因/打回对象/修复结果`、`PM 把 Internal vN 升级为 GM v1 的明确签收语`、`修改文件`、`验证结果`、`最终交付结论`。
- 向用户汇报时，不能只说“已按多 agent workflow 处理”；必须显式给出完整流程记录，并告知对应 `log/` 文件路径，同时明确说明 `PM` 和 `UI` 实际使用了哪些 skills；如果某一角色本轮未使用额外 skills，也要显式写 `none`。
- `log/` 是本地目录，必须加入 `.gitignore`，不得提交到 GitHub。

## 0.5 PM / UI 默认专业 Skills

- `PM` 和 `UI` 角色必须主动利用全局 `.codex/skills` 中的专业 skills，而不是只凭直觉产出方案；但不得机械地每次全用，必须按任务真实需要选择。
- 一旦选择某个 skill，必须先读取该 `SKILL.md` 再使用；不能只在日志里挂名。

### 产品经理 agent 默认优先 skills

- `problem-statement`：先把用户需求改写成真实用户问题，避免直接拿功能点开做。
- `jobs-to-be-done`：当用户需求背后动机、痛点、收益不够清楚时使用。
- `proto-persona`：当目标用户画像模糊、需要统一“为谁设计”时使用。
- `opportunity-solution-tree`：当需求像老板指令/功能点，需要先拆“结果 -> 机会点 -> 方案”时使用。
- `prioritization-advisor`：当需求范围过大、需要决定先做什么/不做什么时使用。
- `prd-development`：当任务是中大型功能、需要正式产品交接文档时使用。
- `user-story`：当需要把产品要求转成研发可执行、可测试的故事与验收条件时使用。
- `user-story-mapping`：当需求涉及完整用户流程、MVP 切片或多步骤工作流时使用。

### UI 设计师 agent 默认优先 skills

- `design-brief`：先把设计问题、目标、约束、成功标准写清楚。
- `wireframe-spec`：当需要明确布局、内容优先级、组件摆放时使用。
- `user-flow-diagram`：当需要梳理主路径、分支、异常流、恢复流时使用。
- `visual-hierarchy`：当界面层级、主次关系、CTA 权重需要重构时使用。
- `ux-writing`：当按钮、空态、错误态、提示文案影响体验时使用。
- `error-handling-ux`：当流程涉及失败、撤销、恢复、异常反馈时使用。
- `loading-states`：当界面有等待、同步、渐进呈现、骨架屏等状态时使用。
- `design-rationale`：当设计决策需要解释取舍、备选方案与证据时使用。
- `design-critique`：当需要对现有方案做结构化设计复盘与打回时使用。
- `heuristic-evaluation`：当需要按可用性启发式系统检查流程与界面问题时使用。
- `responsive-design`：当任务会影响不同窗口宽度、桌面尺寸和输入方式时使用。
- `accessibility-audit`：当变更会影响可访问性、对比度、键盘操作、语义时使用。
- `design-qa-checklist`：在实现后做设计验收和回归检查时使用。

## 1. 版本与打包

- 除非用户明确提及大版本变动，否则一律做**最小版本升级**（如 `1.0.0` → `1.0.1`，`1.0.1` → `1.0.2`）。
- **同一功能的反复优化和修改，版本号只递进一次**，不重复递进。只有确认该功能完成、进入下一个新功能时才再次递进版本号。
- 版本号在 `package.json` 的 `version` 字段中修改。
- `release/` 目录最多保留 **10 个版本**的文件。打包完成后检查该目录，如果文件数超过 10 个，按修改时间从旧到新排序，删除最老的文件直到剩余不超过 10 个。

## 2. README 同步

- 只同步**大功能 + 主要子功能**到 README。例如："创建卡片"（大功能）、"支持 Markdown 格式"（主要子功能）。
- **不需要同步**细节性/meta 类功能（如卡片磁吸、性能优化等），这类功能不写也无妨。
- 如果修改了**已在 README 中描述的功能**，且描述已不准确，需要更新 README。
- 如果版本号发生了修改，需要更新到README.md的指定位置。

## 3. 功能验证

- 完成新功能或优化后，**必须进行功能验证**，而非仅检查"编译通过"或"应用能启动"。
- 验证方式：启动测试应用（`npm run dev`），**主动构造测试场景**（如创建卡片、分区、标题、连线等），实际操作触发新功能的交互路径，确认行为符合预期。
- 验证完成后清理测试数据，还原到干净状态。
- 如果验证中发现问题，**先修复再重新验证**，循环往复直到功能确认 OK。
- **只有功能验证通过后**，才能执行打包和推送。禁止跳过验证直接发版。
- 验证时要特别注意：**代码逻辑的前置条件在运行时是否成立**（如 DOM 事件能否到达目标元素、CSS 是否阻断了交互、数据流是否完整等），不能仅凭代码静态分析判断正确性。

## 4. 第一性原理执行清单

### 开工前先拆本质

- 先回答“用户真正想更快、更稳、更舒服地完成什么”，再回答“要不要做这个按钮、入口、动画、结构”。
- 每次开工前都要明确 5 件事：
  1. 用户是谁，当前场景是什么？
  2. 用户要完成的最小任务是什么？
  3. 方案里哪些步骤是必要的，哪些只是历史包袱、实现习惯或视觉装饰？
  4. 用户在理解、点击、等待、回退上的成本分别是什么？
  5. 如果只能保留一个价值点，这里最该保留什么？
- 如果这 5 件事说不清，先不要编码，继续拆。

### 产品与交互标准

- 黏性来自“功能真的好用、低阻力、随时可继续”，不是广告、强制任务、伪成长、骚扰提醒。
- 用户必须快速看懂：`我现在在哪里`、`下一步做什么`、`做完会发生什么`、`出错后如何恢复`。
- 主路径优先追求更少点击、更少切换、更少思考；能合并的步骤不要拆开，能默认的参数不要强迫用户先配置。
- 交互不合理时，不要靠提示文案补洞，先回到流程本身重设计。

### UI 标准

- FloatAnchor 的气质是：简洁、安静、专业、本地优先、可长期使用，不做花哨、不做噪音。
- 优先用留白、层级、对齐、对比、节奏解决问题，不用堆颜色、堆装饰、堆弹层制造“设计感”。
- 新 UI 必须与现有主题变量、圆角、阴影、字号层级、模块结构保持连续性；除非收益非常明确，不要引入另一套视觉语言。
- Light / Dark 两套主题都要成立，桌面端（macOS / Windows）的操作路径都要顺手。

### 实现标准

- 先找最小可解释的数据流、最少必要抽象、最短主路径；不要为“以后可能会用到”提前制造复杂层。
- 一旦实现让你觉得别扭、绕、补丁味重，停止继续堆代码，回到：事实、约束、目标、最简单可行解。
- 所有关键前置条件都必须在运行时验证，不能只靠静态阅读自我说服。
- 输出方案时，要明确写出：`用户目标`、`核心取舍`、`删掉了什么复杂度`、`为什么这是当前最简单正确的解`。
